services:
  genesys-license-tracker:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - tracker-data:/app/data
    restart: unless-stopped

volumes:
  tracker-data:
